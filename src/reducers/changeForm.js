import * as Types from '../constants/ActionTypes'

let initialstate = 'signin'

const changeForm = (state = initialstate, ations) => {
    switch (actions.type) {


        default:
            return state
    }
}